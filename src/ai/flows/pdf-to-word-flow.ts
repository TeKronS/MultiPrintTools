
'use server';
/**
 * @fileOverview Este flujo ha sido desactivado para priorizar la privacidad y el procesamiento local sin costo.
 */
export async function convertPdfToWordStructure(input: { pdfDataUri: string }): Promise<any> {
  return { pages: [] };
}
